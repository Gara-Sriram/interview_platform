import { Inngest } from "inngest";

export const inngest = new Inngest({ id: "talent-iq" });

// Inngest functions placeholder — Clerk user sync removed.
// Add new background job functions here as needed.
export const functions = [];
